# Design Dent — tablet/mobile stage 21

Исправлено:
- карта больше не ищет организацию по текстовой строке;
- iframe переведён на официальный Yandex Business object mode с `oid=1253824945` и `ol=biz`;
- центр карты зафиксирован на координатах клиники, zoom 17;
- это должно показывать именно метку/карточку «Дизайн Дент» и на мобильных устройствах;
- после активации карты доступна небольшая fallback-ссылка «Открыть Дизайн Дент в Яндекс Картах».

- на ширине <=620px для `.hero__card` задан `min-height: 650px`.

- hero на ширине <=920px переведён с конфликтующих абсолютных координат на потоковую композицию: `hero__content` и `hero__tags` больше не перекрывают друг друга;
- на <=620px сохранён `min-height: 650px`, но высота может увеличиваться по контенту;
- фон hero заменён на `o3dpJW3O48.png` с действующего сайта клиники;
- если внешний файл изображения временно недоступен, предусмотрен fallback на локальный `assets/img/hero.webp`.

- возвращено прежнее локальное изображение `assets/img/hero.webp` в hero;
- в tablet/mobile header телефон, email и burger приведены к одной системе размеров;
- иконки телефона и email теперь чёрные;
- размеры glyph-иконок и burger-линий выровнены визуально.

- для ширины <=920px добавлен финальный specificity override: иконки телефона/почты и burger принудительно используют `var(--text)` вместо золотого цвета.

- исправлен фактический selector mobile-контактов: `.header__mobile-contacts a`;
- у телефона и почты на <=920px теперь `color: var(--text) !important`;
- SVG продолжает использовать `stroke: currentColor`, поэтому иконки наследуют правильный чёрный цвет;
- размеры mobile contact icons унифицированы: 20px на tablet и 19px на <=620px.
